class HttpConfig {
  static const url = 'https://xiaomi.itying.com';
  static const baseUrl = '$url/api';
  static const connectTimeout = Duration(seconds: 5);
  static const receiveTimeout = Duration(seconds: 5);
}
